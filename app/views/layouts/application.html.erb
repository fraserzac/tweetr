<!DOCTYPE html>
<html>
<head>
  <title>Tweetr</title>
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>

<nav>
  <div class="title">
  <h1>Tweetr</h1>
  </div>

  <div class="container">
	<% if @current_user.present? %>
    	<%= link_to 'Home', posts_path %>
      <%= link_to "Logout #{ @current_user.name }", login_path, :method => 'delete', :data => { :confirm => 'Are you sure you wish to logout?' } %>
      <%= link_to 'Account', new_user_path %>
      <%= link_to 'Followed Users', followed_path %>
    <% else %>
  		<%= link_to 'Login', login_path %>
  		<%= link_to 'Join Tweetr', new_user_path %>

<% end %>

  	</nav>


<%= yield %>
</div>


</body>
</html>
